🧱 1. Build the package locally
In the root of your django-csvfixtures folder (the one containing pyproject.toml):

bash
￼Copiar código
python -m build
This will create:

dist/
├── django_csvfixtures-1.0.0.tar.gz
├── django_csvfixtures-1.0.0-py3-none-any.whl

If you don’t have the build package yet:

pip install build

🧪 2. Install it in your Django project (locally)

In your test Django project’s virtual environment, run:

pip install /absolute/path/to/django-csvfixtures/dist/django_csvfixtures-1.0.0-py3-none-any.whl

Example:

pip install ~/Projects/django-csvfixtures/dist/django_csvfixtures-1.0.0-py3-none-any.whl
✅ This simulates a real PyPI install.

If you’re actively developing the library and want to auto-reload changes, use an editable install:

pip install -e /absolute/path/to/django-csvfixtures
This links your Django project directly to the local source code — any edit in the library will immediately reflect in
your test project.